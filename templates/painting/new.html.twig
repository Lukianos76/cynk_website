<!DOCTYPE html>

<title>New Painting</title>

{% block body %}
    <h1>Create new Painting</h1>

    {{ include('painting/_form.html.twig') }}

    <a href="{{ path('painting_index') }}">back to list</a>
{% endblock %}
