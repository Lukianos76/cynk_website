<form method="post" action="{{ path('painting_delete', {'id': painting.id}) }}" onsubmit="return confirm('Êtes vous sur de vouloir supprimer ce tableau ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ painting.id) }}">
    <button class="uk-button uk-button-danger"><i class="fas fa-trash-alt"></i></button>
</form>
