<!DOCTYPE html>

<title>Edit Painting</title>

{% block body %}
    <h1>Edit Painting</h1>

    {{ include('painting/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('painting_index') }}">back to list</a>

    {{ include('painting/_delete_form.html.twig') }}
{% endblock %}
